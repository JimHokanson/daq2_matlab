classdef saved_daq_chan < handle
    %
    %   Class:
    %   daq2.explore.saved_daq_chan
    
    properties
        h
        short_name
        name
        fs
    end
    
    methods
        function obj = saved_daq_chan(h,analog_input_chan)
            %
            %   obj = daq2.explore.saved_daq_chan(h,analog_input_chan)
            
        end
    end
end

